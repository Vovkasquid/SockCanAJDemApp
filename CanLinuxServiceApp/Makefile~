PRG = CanLinuxServiceApp

CFLAGS = -O0 -g -Wall -static -DAJ_MAIN -I../ajtcl/dist/include -I../ajtcl/src/target/linux/
LDFLAGS = -L../ajtcl/build/debug/src/ -lajtcl -lpthread -lcrypto

CC = gcc
$(PRG): $(PRG).o
	$(CC) $(CFLAGS) $? $(LDFLAGS) -o $@

clean:
	rm -f $(PRG) *.o
